mode: ContinuousDeployment
tag-prefix: ""
commit-message-incrementing: Disabled

branches:
  main:
    regex: ^main$
    label:
    increment: Patch

  develop:
    regex: ^develop$
    label: alpha
    increment: Patch

  feature:
    regex: '^features?[\/-](?<BranchName>.+)'
    mode: ContinuousDeployment
    label: "{BranchName}"
    increment: Patch
