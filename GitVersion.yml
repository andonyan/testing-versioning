mode: ContinuousDeployment
tag-prefix: ""
commit-message-incrementing: Enabled

branches:
  main:
    regex: ^main$
    increment: Patch
    label: ""

  develop:
    regex: ^develop$
    mode: ContinuousDeployment
    increment: Patch
    label: alpha

  feature:
    regex: '^features?[\/-](?<BranchName>.+)'
    increment: Patch
    label: "{BranchName}"
